# RomesKML_converter — состояние ошибок

Отчёт от 19.08.2026. Разбор от 17.08.2026 перепроверен по коду целиком, после чего
исправлены **все 24 пункта**, кроме одного, оставленного осознанно (см. последний раздел).

Проверка: `gcc 15.2 (MinGW-w64)`, сборка обеих версий с `-Wall` без единого предупреждения;
полный прогон `170.kml` (17 к точек, 6 папок) в режимах TOT и SEP; синтетические тесты на
пропуски данных; сверка выхода консольной и оконной версий.

---

## Что изменилось в результатах

Из десяти выходных файлов **восемь остались побайтово прежними**: `170.csv`,
`170_coverchart.csv`, все пять kml по операторам и `170_rawcov.kml`. Изменились два.

**`Coverage_170.csv`** — файл стал внутренне согласованным:

| Величина | Было | Стало |
|---|---|---|
| сумма участков в таблице | 320.552364 | 320.552150 |
| строка `uncovered distance` | 320.647378 | 320.552150 |
| `total distance` | 339.906005 | 339.905781 |
| `covered distance` | 19.258627 | 19.353631 |

Раньше таблица участков бралась из усреднённого расчёта, а итоги — из сырого, поэтому
сумма строк не сходилась с итогом на 95 метров. Теперь и то и другое — усреднённый расчёт,
сумма участков совпадает с итогом до последнего знака. Сырое значение не потеряно: оно
добавлено отдельной строкой `uncovered distance without distance averaging` (320.647163).

Изменение общей длины на **−0.000223 км** (22 см на 340 км) — следствие уточнения π.
Направление проверено отдельным расчётом по `170.csv`: на широте 66° уменьшение `cos(lat)`
перевешивает рост синусов, поэтому точное π даёт чуть меньшую длину, а не большую.

**`170_avgcov.kml`** — отличается только подписью: `RAW coverage data` → `AVG coverage data`.
Ни координаты, ни цвета точек не изменились (0 строк с другими отличиями).

---

## A. Искажение результатов — исправлено

| № | Что сделано |
|---|---|
| A1 | `savecsvcovtab()`: участки и итоги в режиме TOT берутся из одного прохода (усреднённого); сырой итог добавлен отдельной строкой |
| A2 | Цикл формирования участков усреднённого покрытия доведён до `j <= last`, повторное добавление последнего сегмента после цикла убрано |
| A3 | В режиме SEP координаты участков берутся из своей папки, а не из `folder[0]` |
| A4 | Пустая ячейка csv больше не читается как 0 dBm: появилось поле `hasdata`, пропуски исключены из усреднения и покрытия, координаты подставляются из трека вместо (0,0) |
| A5 | Усреднённое покрытие переехало из `folder[1]` (реальная папка оператора) в зарезервированный слот `folder[folder_quantity]` |
| A6 | π = 3.14159265358979323846 вместо 3.14159, радиус вынесен в `EARTH_RADIUS` |
| A7 | Покрытие последней точки вычисляется по её собственному уровню — последняя точка обрабатывается в общем цикле |
| A8 | В `_coverchart.csv` шапка и строки данных фильтруют GPS-папку одинаково (`"GPS Data"`) |
| A9 | В `_avgcov.kml` подпись `AVG coverage data` |

## B. Устойчивость и память — исправлено

| № | Что сделано |
|---|---|
| B1 | `scanf("%d")` проверяет результат и чистит буфер — ввод буквы больше не зацикливает меню |
| B2 | Все `%s` ограничены шириной: `%199s` для имени файла, `%99s` в разборе settings.ini |
| B3 | Введена `appendpath()`; шесть вызовов `strncat(path, ..., 400)` в буфер размера 400 заменены на неё |
| B4 | В `savecsv()` индекс папки проверяется против её длины — чтения за концом массива больше нет |
| B5 | `placemark_t` обнуляется перед разбором |
| B6 | Отсутствие `" Measurement #"` в имени больше не приводит к разыменованию NULL |
| B7 | Введена `copy_tag_text()`: длина всегда обрезается по размеру приёмника, буфер увеличен до 400 |
| B8 | Цвет читается как `%9s` в массив `color[10]` |
| B9 | Проверки результата `malloc` во всех местах выделения |
| B10 | Появилась `gpsfolderindex()`: папка трека ищется по имени, а не считается нулевой |

## C. Производительность и косметика — исправлено

| № | Что сделано |
|---|---|
| C1 | `fprintf(файл, не_литерал)` заменены на `fputs` (5 мест) |
| C2 | `getmedian()` использует `qsort` вместо сортировки выбором O(n²) |
| C3 | `system("mkdir ...")` заменён на `_mkdir()` |
| C4 | Опечатка в имени файла: пишется `Инструкция.txt` |
| C5 | Запись `file_array[str_max]` за границей массива, `%d` для `u32`, `%u` для `int`, `u32` в размере памяти (теперь `size_t`), неосвобождаемая память при выходе, неиспользуемые переменные, предупреждения `-Wall` |

---

## Что осталось осознанно

**Экранирование `;` в csv.** Имя папки, содержащее точку с запятой, развалит структуру
файла при обратном чтении. Не исправлено намеренно: любое экранирование меняет формат
csv, и старые выгрузки перестанут читаться этой же программой. В реальных выгрузках Romes
такого символа в именах нет.

**Отмена обработки в окне.** Кнопка на время работы блокируется, прервать конвертацию
нельзя — для этого нужны проверки внутри циклов парсера, то есть вмешательство в логику.

---

## Как это проверялось

1. **A/B-прогон на `170.kml`** — сборка до и после правок, побайтовое сравнение всех
   выходных файлов. Изменились только те два файла и ровно так, как описано выше.
2. **Режим SEP** на реальных данных: пять отчётов по операторам, итоги сходятся
   (2.358709 + 337.547073 = 339.905782 при общей длине 339.905781).
3. **Пропуски данных**: синтетический csv из 60 точек, где измерения есть только в первых
   десяти. Раньше 50 пустых точек засчитывались покрытыми (0 dBm выше порога) и координаты
   уезжали в (0,0). Теперь: covered 2.224 км, uncovered 10.897 км, общая длина 13.121 км
   соответствует геометрии трека.
4. **Оконная версия против консольной** — все десять файлов идентичны.
5. **Кодировки**: `main.c`, `save_file.c`, `src/gui/*.c` остались в windows-1251, во всех
   файлах CRLF. Двухбайтовый знак градуса `C2 B0` в шаблоне kml сохранён — это отдельная
   ловушка: повторный проход `iconv` схлопывает его в один байт и портит все выгрузки.

## Как повторить регрессию

```bash
gcc -std=c99 -O2 src/*.c -o RomesKML_converter.exe
```

Прогон в отдельной папке, куда скопированы `170.kml` и `settings.ini`:

```bash
printf '1\n170\n\n' | ./RomesKML_converter.exe
```

Затем `cmp` каждого файла из `Output_CSV_files` и `Output_KML_files` с эталонным прогоном.
Полный проход занимает около минуты.
